import 'package:flutter/material.dart';

class RemovedChatScreen extends StatelessWidget {
  const RemovedChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
