import 'package:flutter/material.dart';

class RemovedItemScreen extends StatelessWidget {
  const RemovedItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
